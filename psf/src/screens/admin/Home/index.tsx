import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function HomeAdminScreen() {
  return (
    <SafeAreaView>
      <Text>Hello from page home admin</Text>
    </SafeAreaView>
  );
}
